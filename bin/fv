#!/usr/bin/env bash

set -euo pipefail

#find -type f
file=$(find -type d -name '.git' -prune -o \
	-type d -name 'node_modules' -prune -o \
	-type f -print |
	fzy)
[[ ! -e $file ]] && exit 1

exec vim "$file"
