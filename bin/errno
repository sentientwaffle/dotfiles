#!/usr/bin/env bash

set -e

if [[ -z $1 ]]; then
	echo "usage: ${0##*/} <errno>" >&2
	exit 1
fi

\grep "	$1	" '/usr/include/asm-generic/errno.h'

# From stackoverflow:
#perl -MPOSIX -e 'print strerror($ARGV[0])."\n";' "$1"
