#!/usr/bin/env bash

usage() {
	cat <<-EOF
	usage: ${0##*/} <outfile> <infile>...

	Join several PDFs together.
	EOF
}

if [ $# -lt 3 ]; then
	usage >&2
	exit 1
fi

# TODO maybe `shift` first argument?
outfile=$1
infiles="${@:2}"
mutool merge -o "$outfile" $infiles
