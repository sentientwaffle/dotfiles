#!/usr/bin/env node

var CLEAR = '\u001b[39m'
  , str   = ""

for (var i = 0; i < 256; i++) {
  if (i && i % 32 === 0) str += "\n"
  str += color(i) + "X" + CLEAR
}

console.log(str)

function color(n) { return '\u001b[38;5;' + n + 'm' }
